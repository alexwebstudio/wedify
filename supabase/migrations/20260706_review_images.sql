-- ============================================================
-- Wedify — патч 1.1.2: изображения в отзывах
-- Запусти это в Supabase SQL Editor.
-- Код работает и без этой миграции (просто без картинок),
-- но чтобы отзывы сохраняли прикреплённые фото — выполни её.
-- ============================================================

ALTER TABLE public.reviews
  ADD COLUMN IF NOT EXISTS images JSONB NOT NULL DEFAULT '[]'::jsonb;
